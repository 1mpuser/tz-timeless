import React from 'react';
import classes from './StatComponent.module.css';

const StatComponent = () => {
	return (
		<div className={classes['stat-ovr']}>
			<div className={classes['stat-big-label']}>krek</div>
			<div className={classes['stat-big-label']}>krek</div>
			<div className={classes['stat-big-label']}>krek</div>
		</div>
	);
};

export default StatComponent;
